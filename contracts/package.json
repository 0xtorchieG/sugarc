{
  "name": "contracts",
  "private": true,
  "scripts": {
    "build": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "deploy:pool-vault": "hardhat run scripts/deploy-pool-vault.ts --network localhost",
    "deploy:pool-vault:arc": "hardhat run scripts/deploy-pool-vault.ts --network arcTestnet",
    "test": "hardhat test",
    "dev": "hardhat node"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "typescript": "^5.3.0"
  }
}
