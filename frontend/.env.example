# Circle Wallets (see README for setup)
CIRCLE_API_KEY=
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
NEXT_PUBLIC_CIRCLE_APP_ID=

# Optional: override Circle API base URL
# NEXT_PUBLIC_CIRCLE_BASE_URL=https://api.circle.com

# Invoice funding (BE-003): Circle developer-controlled wallet as operator
# 1. Register entity secret: https://developers.circle.com/wallets/dev-controlled/register-entity-secret
# 2. Create wallet set + wallet on ARC-TESTNET
# 3. Set operator on SugarcPoolVault to the wallet address
ENTITY_SECRET=
OPERATOR_WALLET_ID=

# Optional: Arc testnet RPC for receipt parsing (default: https://rpc.testnet.arc.network)
# ARC_RPC=https://rpc.testnet.arc.network

# Demo: show "Simulate payment received" button on SMB factored invoices (BE-006)
# NEXT_PUBLIC_DEMO_MODE=true

# PDF extraction: Gemini AI (primary). If unset or rate limited, falls back to OpenAI then heuristics.
# Uses gemini-1.5-flash. Get key: https://aistudio.google.com/apikey
# GEMINI_API_KEY=

# PDF extraction fallback: OpenAI (optional). Used when Gemini fails. Uses gpt-4o-mini.
# OPENAI_API_KEY=

# Payer notification email after funding. If unset, email payload is logged to console.
# RESEND_API_KEY=
# RESEND_FROM_EMAIL=Sugarc <onboarding@resend.dev>
